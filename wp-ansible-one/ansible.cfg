[defaults]
remote_user = ansible
host_key_checking = false
inventory = inventory
deprecation_warnings = False

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = False

